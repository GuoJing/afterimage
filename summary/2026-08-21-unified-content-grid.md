# 全站内容网格统一交接

- 首页、文章详情、独立页面和归档页统一使用 `1200px` 外层内容边界。
- 带日期的内容统一使用 `112px` 日期栏、`24px` 间距和最大 `760px` 正文栏。
- 日期仍与站点标题、全局导航左边缘对齐；正文起点从外层左边缘向右 `136px`，明显缩短了日期与标题之间的空白。
- 独立页面没有日期栏，但标题与正文从统一正文列开始，宽度同样为 `760px`。
- 首页首篇文章和后续文章列表、归档列表都复用相同的列宽与对齐关系。
- 小屏幕下所有页面恢复单列，日期位于标题上方，左右留白为 `16px`。
