# 文章分类枚举

- 后台新建和编辑文章统一使用分类下拉框，选项顺序固定为 `Post`、`HOWILEARN`、`Interview`、`Friends`。
- 新建文章默认选择 `Post`；编辑文章会根据数据库中完全一致的英文分类值自动选中。
- 服务端以相同枚举进行严格校验，拒绝空值、未知分类和大小写错误的值，数据库继续使用原有 `TEXT` 字段并保存枚举原始大小写。
- 本次不涉及数据库结构调整，无需执行 SQLite 迁移。
