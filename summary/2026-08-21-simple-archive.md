# 归档页简化交接

- `/archive` 不再显示 `POSTS`、归档页大标题和说明文字，只保留文章列表。
- 页面标题、描述、canonical、hreflang 和结构化数据仍保留在 HTML `<head>` 中，不影响 SEO 和多语言。
- 第一条文章直接进入统一的内容网格：日期栏 `112px`、栏间距 `24px`、正文栏最大 `760px`。
- 桌面端日期继续与 Banner 左边缘对齐；移动端保持单列和 `16px` 左右留白。
