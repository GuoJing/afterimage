# 标题左对齐与日期弱化交接

- 除归档页外，首页、文章详情、首页后续文章列表和独立页面不再使用日期栏网格。
- 这些页面的标题、正文都与页头 Logo 和全局导航左边缘对齐，内容最大宽度由 `760px` 扩大为 `896px`。
- 文章日期移动到标题下方，使用 `12px`、较浅灰色和正常字重，作为低强调辅助信息。
- 归档页保持原有 `112px` 日期栏、`24px` 间距、`760px` 正文栏，因为归档需要突出时间顺序。
- 移动端仍使用 `16px` 左右留白，内容自然占满可用宽度。
