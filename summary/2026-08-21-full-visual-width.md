# 全页视觉宽度与顶部间距交接

- 公开内容区域以页头 Logo 左边缘为左界、明暗模式按钮右边缘为右界，与页头共同使用完整 `1200px` 视觉宽度。
- 首页、文章详情、首页文章列表和独立页面的内容最大宽度由 `896px` 扩大到 `1200px`。
- Archive 继续保留 `112px` 日期栏和 `24px` 间距，但正文列会占用剩余宽度，使列表右边缘同样对齐明暗模式按钮。
- 导航下方到首个标题的桌面留白由约 `94px` 缩短到约 `44px`。
- 移动端首个标题的顶部留白同步缩短，左右仍保持 `16px`。
