# Post 图片纵向间距

- 首页最新文章正文与 Post 详情共用 `.prose` 图片规则，因此本次同时修复两个页面。
- 同一行并排图片继续使用 `6px` 横向间距，保持组合图片的整体感。
- 上下排列的图片使用响应式 `18–28px` 纵向间距；手机端固定为 `18px`。
- 连续 Markdown 图片组不再使用 `margin-top:-1.5em` 压缩空白，改为正常的 `2em` 顶部间距。
- 不改变图片宽度、原始比例、灯箱或懒加载行为。
