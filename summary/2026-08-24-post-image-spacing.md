# Post 图片纵向间距

- 首页最新文章正文与 Post 详情共用 `.prose` 图片规则，因此本次同时修复两个页面。
- 同一行并排图片和上下排列图片统一使用 `14px` 间距，避免左右过窄、上下过宽。
- 连续 Markdown 图片组会清除前一组的底部外边距，再使用 `14px` 顶部间距，确保视觉缝隙与同组图片一致。
- 不改变图片原始比例、灯箱或懒加载行为。
- 单张上下排列图片恢复为内容区 `100%` 宽度，所有图片继续铺满可用行宽；本次只调整图片之间的缝隙。
